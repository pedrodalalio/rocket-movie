import styled from "styled-components";

export const Container = styled.div`
    .linkContent{
        display: flex;
        color: ${({ theme }) => theme.COLORS.PINK};
        align-items: center;
    }
`